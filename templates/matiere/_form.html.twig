{{ form_start(form) }}
<div class="form-group">
    {{ form_label(form.name) }}
    {{ form_widget(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Nom de la matière'}}) }}
</div>
<div class="form-group">
    {{ form_label(form.intervenant) }}
    {{ form_widget(form.intervenant, {'attr': {'class': 'form-control'}}) }}
</div>
<div class="form-group">
    {{ form_label(form.classe) }}
    {{ form_widget(form.classe, {'attr': {'class': 'form-control'}}) }}
</div>
<button class="btn btn-primary btn-block mb-4">{{ button_label|default('Save') }}</button>

<p>Votre intervenant ou classe n'existe pas ? Créer le !</p>
<a title="créer intervenant" href="{{ path('app_intervenant_new') }}">Créer un intervenant</a><br>
<a title="créer classe" href="{{ path('app_classe_new') }}">Créer une classe</a>



{{ form_end(form) }}
