{% extends 'base.html.twig' %}

{% block title %}Matiere index{% endblock %}

{% block body %}
    <h1>Matiere index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for matiere in matieres %}
            <tr>
                <td>{{ matiere.id }}</td>
                <td>{{ matiere.name }}</td>
                <td>
                    <a href="{{ path('app_matiere_show', {'id': matiere.id}) }}"><i class="fa-regular fa-eye" style="margin-right: 15px;"></i></a>
                    <a href="{{ path('app_matiere_edit', {'id': matiere.id}) }}"><i class="fa-solid fa-pen" style="margin-right: 15px;"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_matiere_new') }}">Créer <i class="fa-solid fa-plus"></i></a>
{% endblock %}
