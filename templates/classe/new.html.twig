{% extends 'base.html.twig' %}

{% block title %}Ajouter une classe{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ block('title') }}</div>
                    <div class="card-body">
                        <h1 class="mb-4">{{ block('title') }}</h1>
                            {{ include('classe/_form.html.twig') }}
                        <a href="{{ path('app_classe_index') }}" class="btn btn-secondary mt-4">Back to List</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

