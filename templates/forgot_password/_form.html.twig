{# ca dcp je pense faut faire un truc dans la BDD et un controller bien sur qui genere le form avec un forgotpasswordType #}

{{ form_start(form) }}
<div class="form-group">
    {{ form_label(form.plainPassword.first) }}
    {{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control', 'placeholder': 'Nouveau mot de passe'}}) }}
</div>
<div class="form-group">
    {{ form_label(form.plainPassword.second) }}
    {{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control', 'placeholder': 'Confirmer le nouveau mot de passe'}}) }}
</div>
<button class="btn-primary btn-block mb-4">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
